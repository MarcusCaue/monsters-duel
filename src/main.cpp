#include "./Game.hpp"

int main() {	
	// Teste t = Teste(Battle());

	Game::run();
	// Jogador playerOne = Jogador("Pedro");
  // Jogador playerTwo = Jogador("Pedro");
  // ChoicePlayers cp = ChoicePlayers("Fase para cadastro de Jogadores", SpriteBuffer(), playerOne, playerTwo);

	// vector<Fase*> subFases;
	// subFases.push_back(&cp);

	// cout << (*(subFases[0])).getName() << endl;

	return 0;
}